<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .header {
            background-color: #f8f9fa;
            padding: 20px;
            border-radius: 8px;
            margin-bottom: 20px;
        }
        .header h2 {
            margin: 0;
            color: #2c3e50;
        }
        .details {
            background-color: #ffffff;
            border: 1px solid #e9ecef;
            padding: 20px;
            border-radius: 8px;
            margin-bottom: 20px;
        }
        .detail-row {
            margin-bottom: 10px;
            display: flex;
            align-items: baseline;
        }
        .detail-label {
            font-weight: 600;
            min-width: 100px;
            color: #495057;
        }
        .detail-value {
            color: #212529;
        }
        .transcription-box {
            background-color: #f8f9fa;
            border: 1px solid #dee2e6;
            padding: 15px;
            border-radius: 8px;
            margin: 20px 0;
            white-space: pre-wrap;
            word-wrap: break-word;
            font-family: 'Consolas', 'Monaco', 'Courier New', monospace;
            font-size: 14px;
        }
        .disclaimer {
            font-size: 12px;
            color: #6c757d;
            font-style: italic;
            margin-top: 20px;
            padding: 10px;
            background-color: #fff3cd;
            border: 1px solid #ffeeba;
            border-radius: 4px;
        }
        .footer {
            margin-top: 30px;
            padding-top: 20px;
            border-top: 1px solid #e9ecef;
            font-size: 12px;
            color: #6c757d;
            text-align: center;
        }
        .alert {
            padding: 12px 20px;
            margin: 20px 0;
            border-radius: 4px;
            font-weight: 500;
        }
        .alert-warning {
            background-color: #fff3cd;
            color: #856404;
            border: 1px solid #ffeeba;
        }
        .alert-danger {
            background-color: #f8d7da;
            color: #721c24;
            border: 1px solid #f5c6cb;
        }
        @media (max-width: 600px) {
            body {
                padding: 10px;
            }
            .detail-row {
                flex-direction: column;
            }
            .detail-label {
                margin-bottom: 5px;
            }
        }
    </style>
</head>
<body>
    <div class="header">
        <h2>{{TITLE}}</h2>
    </div>
    
    <div class="details">
        <p>Hello {{VM_NAME}},</p>
        <p>You have received a new voicemail message.</p>
        
        <div class="detail-row">
            <span class="detail-label">Date:</span>
            <span class="detail-value">{{VM_DATE}}</span>
        </div>
        <div class="detail-row">
            <span class="detail-label">From:</span>
            <span class="detail-value">{{VM_CALLERID}}</span>
        </div>
        <div class="detail-row">
            <span class="detail-label">Mailbox:</span>
            <span class="detail-value">{{VM_MAILBOX}}</span>
        </div>
        <div class="detail-row">
            <span class="detail-label">Message #:</span>
            <span class="detail-value">{{VM_MSGNUM}}</span>
        </div>
        <div class="detail-row">
            <span class="detail-label">Duration:</span>
            <span class="detail-value">{{VM_DURATION}} seconds</span>
        </div>
    </div>
    
    {{#if TRANSCRIPTION}}
    <div class="details">
        <h3>Transcription</h3>
        <div class="transcription-box">{{TRANSCRIPTION}}</div>
    </div>
    {{/if}}
    
    {{#if VM_ALERT}}
    <div class="alert {{VM_ALERT_CLASS}}">
        {{VM_ALERT}}
    </div>
    {{/if}}
    
    <div class="disclaimer">
        <strong>Notice:</strong> This transcription is provided as a convenience and may not be 100% accurate. 
        Please listen to the attached audio recording for the complete message.
    </div>
    
    <div class="footer">
        <p>{{FOOTER}}</p>
        {{#if TRACKING}}
        <img src="{{TRACKING}}" width="1" height="1" style="display:none;" alt="">
        {{/if}}
    </div>
</body>
</html>